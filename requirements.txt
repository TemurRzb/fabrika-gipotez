# Общие
jsonschema>=4.20
python-dotenv>=1.0
pytest>=8.0

# ingestion
python-docx>=1.1
pdfplumber>=0.11
openpyxl>=3.1
# OCR сканов/фото (png/jpg) и сканированных страниц pdf.
# Требует системный движок Tesseract (см. modules/ingestion/README.md, раздел OCR):
#   Windows: winget install --id UB-Mannheim.TesseractOCR
#   Codespaces/Debian: sudo apt-get install -y tesseract-ocr tesseract-ocr-rus
pytesseract>=0.3.10
Pillow>=10.0
# рендеринг страниц pdf в изображение для OCR сканированных pdf (без системных зависимостей типа poppler)
pypdfium2>=4.30

# rag_core
scikit-learn>=1.4
numpy>=1.26

# hypothesis_gen
openai>=1.30

# ranking
# (numpy уже объявлен выше)

# orchestrator_ui
streamlit>=1.33
pandas>=2.2

# используется в тестах ingestion для генерации фикстур .pdf
reportlab>=4.1
