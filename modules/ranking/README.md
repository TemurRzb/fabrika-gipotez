# ranking

Ранжирование и обоснование гипотез, преимущественно НЕ-LLM методами (эмбеддинг-близость,
rule-based скоринг).

**Вход:** `raw_hypotheses` ([`schemas/raw_hypotheses.schema.json`](../../schemas/raw_hypotheses.schema.json)) + `constraints`.
**Выход должен соответствовать** [`schemas/ranked_hypotheses.schema.json`](../../schemas/ranked_hypotheses.schema.json).

## Статус скелета

Функция `rank(raw_hypotheses: dict, constraints: dict) -> dict` в [`rank.py`](rank.py):

| Score | Метод сейчас | TODO |
|---|---|---|
| `novelty` | псевдо-эмбеддинги (детерминированные случайные векторы по хэшу текста) + косинусная близость между гипотезами | заменить на реальные эмбеддинги из `rag_core` (Yandex text-embeddings) |
| `feasibility` | rule-based: доля materials/equipment из constraints, упомянутых в тексте | доработать веса, учитывать бюджет |
| `expected_value` | rule-based: парсинг чисел (%) из `target_property_impact` | при необходимости — учитывать несколько чисел/диапазонов |
| `risk` | rule-based: ключевые слова-маркеры риска (патент, автоклав, капзатраты...) | расширить список маркеров под реальные данные |
| `overall` | взвешенная сумма выше (веса в `WEIGHTS`) | подбор весов вместе с командой на реальных данных дня 2 |
| `roadmap` | шаблонные 2 шага (лаб -> пилот) | сделать шаги специфичнее под тип гипотезы |

## TODO по хронологии

1. **(30 мин)** Прогнать `python modules/ranking/rank.py` на моках, свериться с `mock_data/ranked_hypotheses/ranked_hypotheses_example.json`.
2. **(2-3 ч)** Как только `rag_core` подключит реальные эмбеддинги — заменить `_pseudo_embedding` на переиспользование эмбеддингов чанков/гипотез оттуда (важно для честной новизны).
3. **(1-2 ч)** Пересмотреть веса `WEIGHTS` и пороги в `_compute_risk`/`_compute_feasibility` на реальных данных дня 2 вместе с командой (что считать высоким риском в контексте хвостов/флотации).
4. **(1 ч)** Проверить сортировку и устойчивость overall-скора при малом числе гипотез (1-2 шт.) — не должно быть деления на ноль (см. `_compute_novelty_scores` — уже обработано).
5. **(опционально)** Явное дедуплицирование почти одинаковых гипотез (высокая взаимная близость embeddings) перед выдачей в UI.

## MVP на случай нехватки времени

Rule-based скоринг + псевдо-эмбеддинги — уже рабочий MVP, даёт валидный по схеме, отсортированный список. Если не успеете подключить реальные эмбеддинги — не критично для демо, главное чтобы веса/пороги были осмысленными на реальных данных дня 2.

## Рекомендованные библиотеки

- `numpy` — псевдо-эмбеддинги и косинусная близость (уже используется)
- (позже) переиспользование `scikit-learn`/эмбеддингов из `rag_core`, без новых тяжёлых зависимостей

## Как тестировать независимо

```bash
pip install -r requirements.txt
pytest modules/ranking/tests/test_ranking.py -v

python modules/ranking/rank.py
```
